import React from "react";
import { type NextPage } from "next";
import Layout from "../components/Layout";

const blog: NextPage = () => {
  return(
    <div>
      <Layout>

      </Layout>
    </div>
  );
};

export default blog;